{
  "custom/pacman": {
    "format": "󰮯 {}",
    "interval": 1800,
    "exec": "/usr/bin/checkupdates 2>/dev/null | wc -l",
    "on-click": "kitty -e zsh -c 'fastfetch && sleep 0.5 && sudo pacman -Syu; exec zsh'"
  }
}
