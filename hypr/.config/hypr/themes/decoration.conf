source = ~/.config/hypr/themes/current-theme.conf

decoration {
    rounding = 10
    dim_special = 0.3

    active_opacity = 0.80
    inactive_opacity = 0.5

    shadow {
        enabled = false
    }

    blur {
        enabled = true
        size = 3
        passes = 2
    }
}